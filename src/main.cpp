#include "include/objects.h"
#include "include/environment.h"

//Screen dimension constants
const int SCREEN_WIDTH = 640;
const int SCREEN_HEIGHT = 480;


int main( int argc, char* args[] )
{
	/*
    Camera camera = Camera(Point3D(0,0,0), ReferenceFrame3D());
	Cube cube = Cube(Point3D(0,0,20), Vector3D(1,0,1), Vector3D(0,1,0), Vector3D(-1,0,1), Color(200, 60, 220));
	Sdl sdl = Sdl(SCREEN_WIDTH, SCREEN_HEIGHT);
	sdl.clear();

	for (unsigned i; i <cube.faces.size(); i++){
		Triangle2D t1 = Triangle2D(camera, cube.faces.at(i).t1);
		Triangle2D t2 = Triangle2D(camera, cube.faces.at(i).t2);
		sdl.drawTriangle(t1);
		sdl.drawTriangle(t2);
	}

	sdl.show();

*/
	return 0;
}
